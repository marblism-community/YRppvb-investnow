{
  "type": "nextjs",
  "commands": {
    "build": "pnpm run build",
    "start": "database:sync && pnpm run start"
  },
  "commandsWorkspace": {
    "init": "pnpm install && pnpm run global:sync ; pnpm run database:seed",
    "dev": "pnpm run dev"
  }
}
